package com.example.composecharting.data

import androidx.compose.animation.core.StartOffset
import androidx.compose.ui.geometry.Offset


data class OneDimenLine(
    var StartOffset: Offset,
    var EndOffset: Offset,
    var color: Offset
)